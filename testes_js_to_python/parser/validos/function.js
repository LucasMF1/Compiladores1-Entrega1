// Declaracao de funcao com parametros, declaracao local e return.
function soma(a, b) {
  let total = a + b;
  return total;
}

// Chamada apos a declaracao deve continuar sendo aceita.
let resultado;
resultado = soma(1, 2);
